---
title: "test"
runtime: shiny
output:
  html_document:
    code_folding: show
    df_print: paged
    theme: flatly
    toc: true
    number_sections: false
    toc_depth: 2
    toc_float: 
      collapsed: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Benchmarking the accuracy of multiple systems estimation for the quantification of modern slavery

Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed accumsan neque ut diam tincidunt feugiat. Nulla sem enim, molestie sit amet sem ac, feugiat tincidunt mi. Aliquam sodales feugiat orci nec cursus. Cras finibus eu leo vitae malesuada. Aliquam nec lectus maximus, luctus arcu vitae, volutpat mauris. Mauris faucibus rhoncus ipsum, nec gravida lorem lobortis id. Donec vel tortor nec odio ultrices feugiat. Curabitur a pretium sapien. Sed cursus erat a volutpat tempus. Morbi semper magna quis blandit laoreet. Ut leo tellus, pulvinar ac quam et, sagittis aliquam ex. Donec est ante, maximus at fermentum quis, molestie eget nisl. Fusce non nisi urna.

Integer venenatis pretium dictum. Praesent ac dolor porta, imperdiet metus at, dictum tortor. Cras est ipsum, fermentum quis ipsum nec, porta semper sapien. Maecenas convallis urna non ligula tristique, in aliquet purus pellentesque. Nam egestas velit libero, ac iaculis justo sagittis id. Nunc non nisi nisl. Fusce vehicula venenatis eros vitae cursus. Sed venenatis sem sed ligula iaculis malesuada.

Quisque hendrerit lorem sed lacus ultrices condimentum eget vel nisl. Pellentesque habitant morbi tristique senectus et netus et malesuada fames ac turpis egestas. Duis erat quam, bibendum et mauris non, convallis elementum mi. Aliquam erat volutpat. Mauris fringilla sem sollicitudin tellus vestibulum, ac facilisis leo rhoncus. Donec vitae ipsum elit. Integer condimentum egestas justo, ut commodo erat venenatis vel. Aliquam posuere ornare velit nec vestibulum. Orci varius natoque penatibus et magnis dis parturient montes, nascetur ridiculus mus. Praesent dapibus magna at sapien gravida, a efficitur dolor faucibus. Sed id ligula sed purus aliquet varius. Donec placerat neque ut enim convallis rhoncus. Praesent id ligula nec arcu condimentum tincidunt. Curabitur interdum, nibh nec rutrum porttitor, mauris erat posuere orci, at aliquet eros velit ac neque.

## Show data

```{r max.print=10}
source("../input/silverman_code/datasets.R")
library(rmarkdown)

knitr::knit_print
```

<details>
  <summary>Click for Answer</summary>
    ```{r, echo=FALSE, eval=TRUE}
    hist(1:10)
    ```
</details> 


## Some input and outputs {.tabset}

### Some stuff

```{r eruptions, echo=FALSE}
inputPanel(
  selectInput("n_breaks", label = "Number of bins:",
              choices = c(10, 20, 35, 50), selected = 20),
  
  sliderInput("bw_adjust", label = "Bandwidth adjustment:",
              min = 0.2, max = 2, value = 1, step = 0.2)
)
```

### More stuff

```{r}
shinyApp(
  ui = navbarPage("MSE Benchmark", theme = shinytheme("flatly"), 
             
             tabPanel("Data Application",
                      sidebarLayout(
                        sidebarPanel(
                          
                        ),
                        mainPanel(
                          
                        )
                        )
                      ),
             
             tabPanel("Accuracy Estimation",
                      sidebarLayout(
                        sidebarPanel(
                          
                        ),
                        mainPanel(
                          
                        )
                      )
             ),
             
             tabPanel("Generative Models",
                      sidebarLayout(
                        sidebarPanel(
                          
                        ),
                        mainPanel(
                          
                        )
                      )
             )
  ),
  
  server = function(input, output) {
    
  },
  options=list(width="100%", height=600)
  )

```

## More Inputs and Outputs

You can embed Shiny inputs and outputs in your document. Outputs are automatically updated whenever inputs change.  This demonstrates how a standard R plot can be made interactive by wrapping it in the Shiny `renderPlot` function. The `selectInput` and `sliderInput` functions create the input widgets used to drive the plot.


## Embedded Application

It's also possible to embed an entire Shiny application within an R Markdown document using the `shinyAppDir` function. This example embeds a Shiny application located in another directory:

```{r tabsets, echo=FALSE}
shinyAppDir(
  system.file("examples/06_tabsets", package = "shiny"),
  options = list(
    width = "100%", height = 550
  )
)
```

Note the use of the `height` parameter to determine how much vertical space the embedded application should occupy.

You can also use the `shinyApp` function to define an application inline rather then in an external directory.

In all of R code chunks above the `echo = FALSE` attribute is used. This is to prevent the R code within the chunk from rendering in the document alongside the Shiny components.


```{r}
shinyAppDir("../src/", options=list(width="100%", height="800"))
```


